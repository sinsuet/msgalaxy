# Scenario Input Split

Legacy BOM stack directories are being retired.

Active scenario inputs now live under:

- `config/scenarios/*`
- `config/catalog_components/*`
- `config/system/mass/*`

Use `run/run_scenario.py --stack mass --scenario <id>` to resolve the active scenario contract.
